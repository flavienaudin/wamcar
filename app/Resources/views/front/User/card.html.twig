{% extends 'front/Seller/card.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block presentation %}
    <h2>Présentation</h2>
    {% if user.description %}
        <p>{{ user.description|nl2br }}</p>
    {% elseif userIsMe %}
        <p>Présentez-vous en quelques mots.</p>
    {% else %}
        <p>Aucune description n'a été encore renseignée.</p>
    {% endif %}
    {% if userIsMe %}
        <a href="{{ path('front_edit_user_info') }}" class="text-underline">Modifier mes informations</a>
    {% endif %}
{% endblock %}

{% block other_informations %}
    <h2>Son projet</h2>
    <p><strong class="primary-color">Budget de 18 000 €</strong></p>
    <ul class="list-bullet">
        <li>Audi A3 / 2012+ / -60000 km</li>
        <li>BMW Serie 1 / 2013+ / -60000 km</li>
        <li>Mercedes-Benz Classe A / 2013+ / -60000 km</li>
    </ul>
    <p>Je recherche une citadine allemande en version 5 portes. Diesel ou essence. Mes préférences vont pour une couleur
        noire ou gris anthracite.</p>
    <a href="{{ path('inte_user_personal_profil_edit') }}" class="text-underline">Gérer mon projet</a>
{% endblock %}

{% block vehicle %}
    {% if iCanSeeUserVehicles %}
        <div class="separator small-12 section">
            <h2>{{ userIsMe ? "Mes": "Ses" }} véhicules en vente</h2>
            <div class="row">
                {% set balise = 'h3' %}
                {% set vehicleToList = user.vehicles %}
                {% set isProVehicles = false %}
                {% include 'front/Vehicle/Listing/includes/list.html.twig' %}
            </div>

            {% if userIsMe %}
                <div class="small-12 medium-6 large-4">
                    <a href="#{#{ path('front_vehicle_personal_add') }#}" class="button add icon-plus">Ajouter un véhicule</a>
                </div>
            {% endif %}
        </div>
    {% endif %}
{% endblock %}
