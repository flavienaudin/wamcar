<ul class="brand-list">
    {% for membership in app.user.garageMemberships %}
        <li>
            <figure class="block-grid align-middle">
                <span class="brand-logo"><img src="{{ asset('/assets/images/placeholders/brand/logo-toyota.png') }}" alt="{{ membership.garage.name }}"></span>
                <a href="{{ path('front_garage_view', {'id': membership.garage.id}) }}" class="text-underline">{{ membership.garage.name }}</a>
            </figure>
        </li>
    {% endfor %}
</ul>
