{% for message in messages %}
    <div class="{{ videoProjectViewer.visitedAt < message.createdAt ? "unread"}}">
        {{ message.createdAt|localizeddate('short','short') }} <b>{{ message.author.fullname|title }}</b>
        : {{ message.content }}
    </div>
{% endfor %}