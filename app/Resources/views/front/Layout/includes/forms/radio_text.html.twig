{% set class = class|default('') %}

<input type="radio" name="{{ name }}" id="{{ id }}" value="{{ value }}">
<label for="{{ id }}" class="radio-item {{ class }}">
    {{ label }}
    {% include 'front/Layout/includes/svg/check_alt.html.twig' %}
</label>
