<ul id="js-carousel-vehicle" class="js-carousel carousel">
    {% for picture in vehicle.pictures.primary %}
        <li>
            <img src="{{ asset(picture) }}">
            <p class="vehicle-carousel-legend text-center">Lorem ipsum, dolor sit amet consectetur adipisicing.</p>
        </li>
    {% endfor %}
</ul>
<navl id="js-carousel-navigation-vehicle" class="carousel carousel-navigation">
    {% for picture in vehicle.pictures.secondary %}
        <img src="{{ asset(picture) }}">
    {% endfor %}
</nav>
