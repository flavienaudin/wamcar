{% extends ':Mail/Layout:email.html.twig' %}

{% trans_default_domain 'email' %}

{% block title %}{{ 'notifyUserOfPasswordResetTokenGenerated.title'|trans }}{% endblock %}

{% block body %}
    <div>
        {{ 'notifyUserOfPasswordResetTokenGenerated.body'|trans({
            '%resetUrl%': resetUrl,
            '%siteUrl%': siteUrl
        })|raw }}
    </div>
{% endblock %}
